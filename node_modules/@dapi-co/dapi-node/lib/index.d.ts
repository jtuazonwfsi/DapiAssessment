import Auth from './auth';
import Data from './data';
import Payment from './payment';
import Metadata from './metadata';
export * from './helpers';
import { DapiConfig } from './constants/config';
import Operation from './operation';
export default class App {
    auth: Auth;
    data: Data;
    payment: Payment;
    metadata: Metadata;
    operation: Operation;
    private config;
    constructor(config: DapiConfig);
    handleSDKDapiRequests(body: any, headers: any): Promise<any>;
    verifyWebhookSignature(body: any, headers: any, webhookSecret: string): boolean;
}
