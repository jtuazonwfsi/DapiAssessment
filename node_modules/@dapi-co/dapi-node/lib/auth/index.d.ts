import { IExchangeTokenResponse } from '../types/response/exchangeToken';
import { DapiConfig } from '../constants/config';
export default class Auth {
    config: DapiConfig;
    constructor(config: DapiConfig);
    exchangeToken(accessCode: string, connectionID: string): Promise<IExchangeTokenResponse>;
    delinkUser(userSecret: string, appSecret: string): Promise<void>;
}
