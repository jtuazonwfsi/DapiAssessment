import { DapiConfig } from '../constants/config';
import { IGetAccountsMetadataResponse } from '../types/response/getAccountsMetadata';
import { IUserInputs } from '../types/models/IUserInputs';
export default class Metadata {
    private utils;
    private config;
    constructor(config: DapiConfig);
    getAccountsMetadata(accessToken: string, userSecret: string, userInputs?: IUserInputs[]): Promise<IGetAccountsMetadataResponse>;
}
