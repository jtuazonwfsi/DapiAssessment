export declare enum BeneficiaryStatus {
    APPROVED = "approved",
    REJECTED = "rejected",
    CANCELLED = "cancelled",
    PENDING = "waiting_for_confirmation",
    MODIFIED = "modified_for_pending_approval"
}
export declare enum BeneficiaryType {
    SAME = "same",
    LOCAL = "local",
    INTERNATIONAL = "intl",
    OWN = "own"
}
export interface IBeneficiary {
    name: string;
    iban: string;
    accountNumber: string;
    status: BeneficiaryStatus;
    type: BeneficiaryType;
    id: string;
}
export interface IBeneficiaryAddress {
    line1: string;
    line2: string;
    line3: string;
}
