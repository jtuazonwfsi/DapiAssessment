import { DapiConfig } from '../constants/config';
import { IJobResumeResponse } from '../types/response/jobResume';
export default class Operation {
    private utils;
    private config;
    constructor(config: DapiConfig);
    getOperationStatus(operationID: string): Promise<IJobResumeResponse>;
}
