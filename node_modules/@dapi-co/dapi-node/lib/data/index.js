"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const moment = require("moment");
const request_1 = require("../request");
const productUtils_1 = require("../utils/productUtils");
class Data {
    constructor(config) {
        this.config = null;
        this.utils = null;
        this.dapiDateFormat = 'YYYY-MM-DD';
        this.config = config;
        this.utils = new productUtils_1.default();
    }
    getIdentity(accessToken, userSecret, userInputs) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const response = yield request_1.requestGetIdentity(accessToken, userSecret, this.config.appSecret, userInputs);
                return response.data;
            }
            catch (error) {
                return error.response.data;
            }
        });
    }
    getAccounts(accessToken, userSecret, userInputs) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const response = yield request_1.requestGetAccounts(accessToken, userSecret, this.config.appSecret, userInputs);
                return response.data;
            }
            catch (error) {
                return error.response.data;
            }
        });
    }
    getBalance(accessToken, userSecret, accountID, userInputs) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const response = yield request_1.requestGetBalance(accessToken, userSecret, this.config.appSecret, accountID, userInputs);
                return response.data;
            }
            catch (error) {
                return error.response.data;
            }
        });
    }
    getTransactions(accessToken, userSecret, accountID, fromDate, toDate, userInputs) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const dapiFromDate = moment(fromDate, this.dapiDateFormat, true).format(this.dapiDateFormat);
                const dapiToDate = moment(toDate, this.dapiDateFormat, true).format(this.dapiDateFormat);
                const response = yield request_1.requestGetTransactions(accessToken, userSecret, this.config.appSecret, accountID, dapiFromDate, dapiToDate, userInputs);
                return response.data;
            }
            catch (error) {
                if (error.response) {
                    return error.response.data;
                }
                console.error(error);
            }
        });
    }
}
exports.default = Data;
