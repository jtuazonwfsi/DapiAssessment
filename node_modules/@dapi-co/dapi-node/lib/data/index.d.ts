import { IGetIdentityResponse } from '../types/response/getIdentity';
import { IGetAccountsResponse } from '../types/response/getAccounts';
import { IGetBalanceResponse } from '../types/response/getBalance';
import { DapiConfig } from '../constants/config';
import { IGetTransactionsResponse } from '../types/response/getTransactions';
import { IUserInputs } from '../types/models/IUserInputs';
export default class Data {
    private config;
    private utils;
    private dapiDateFormat;
    constructor(config: DapiConfig);
    getIdentity(accessToken: string, userSecret: string, userInputs?: IUserInputs[]): Promise<IGetIdentityResponse>;
    getAccounts(accessToken: string, userSecret: string, userInputs?: IUserInputs[]): Promise<IGetAccountsResponse>;
    getBalance(accessToken: string, userSecret: string, accountID: string, userInputs?: IUserInputs[]): Promise<IGetBalanceResponse>;
    getTransactions(accessToken: string, userSecret: string, accountID: string, fromDate: Date, toDate: Date, userInputs?: IUserInputs[]): Promise<IGetTransactionsResponse>;
}
